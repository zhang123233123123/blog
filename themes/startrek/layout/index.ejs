<!-- Hero Section -->
<section class="hero-section">
    <div class="hero-background">
        <div class="hero-gradient"></div>
    </div>
    
    <div class="hero-content">
        <div class="hero-title">
            <span class="title-line">Journey Through</span>
            <span class="title-line gradient-text">The Digital Cosmos</span>
        </div>
        
        <p class="hero-subtitle">
            Exploring the infinite possibilities of technology, beyond the event horizon of imagination
        </p>
        
        <div class="hero-actions">
            <a href="<%- url_for('/archives') %>" class="btn btn-primary btn-lg">
                <span>Begin Transmission</span>
                <svg class="icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <line x1="7" y1="17" x2="17" y2="7"></line>
                    <polyline points="7,7 17,7 17,17"></polyline>
                </svg>
            </a>
            <a href="<%- url_for('/about') %>" class="btn btn-secondary btn-lg">
                <span>Mission Brief</span>
            </a>
        </div>
        
        <div class="hero-stats">
            <div class="stat">
                <span class="stat-number">∞</span>
                <span class="stat-label">Dimensions</span>
            </div>
            <div class="stat">
                <span class="stat-number">42</span>
                <span class="stat-label">Light Years</span>
            </div>
            <div class="stat">
                <span class="stat-number">2001</span>
                <span class="stat-label">Odyssey</span>
            </div>
        </div>
    </div>
</section>

<!-- Features Section -->
<section class="features-section">
    <div class="features-container">
        <div class="features-header">
            <h2 class="section-title">Mission Objectives</h2>
            <p class="section-subtitle">
                Advanced technological solutions for interstellar exploration
            </p>
        </div>
        
        <div class="features-grid">
            <!-- Mobile Apps -->
            <div class="feature-card">
                <div class="feature-icon">🚀</div>
                <h3 class="feature-title">Spacecraft Control</h3>
                <p class="feature-description">
                    Advanced mobile interfaces for interstellar navigation and communication systems.
                </p>
                <div class="feature-meta">
                    <span>iOS • Android • Quantum OS</span>
                    <span>• 12 Modules</span>
                </div>
            </div>
            
            <!-- Desktop Software -->
            <div class="feature-card">
                <div class="feature-icon">🌌</div>
                <h3 class="feature-title">Mission Control</h3>
                <p class="feature-description">
                    Sophisticated command centers for deep space exploration and data analysis.
                </p>
                <div class="feature-meta">
                    <span>Endurance OS • TARS • CASE</span>
                    <span>• 18 Systems</span>
                </div>
            </div>
            
            <!-- Web Applications -->
            <div class="feature-card">
                <div class="feature-icon">⚛️</div>
                <h3 class="feature-title">Quantum Networks</h3>
                <p class="feature-description">
                    Interdimensional web applications that transcend space-time boundaries.
                </p>
                <div class="feature-meta">
                    <span>Tesseract • Wormhole • Bootstrap</span>
                    <span>• 24 Portals</span>
                </div>
            </div>
            
            <!-- Resources -->
            <div class="feature-card">
                <div class="feature-icon">📡</div>
                <h3 class="feature-title">Data Archives</h3>
                <p class="feature-description">
                    Comprehensive libraries of cosmic knowledge and interstellar communication protocols.
                </p>
                <div class="feature-meta">
                    <span>Stellar Maps • Time Codes • Coordinates</span>
                    <span>• 36 Collections</span>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Latest Posts Section -->
<section class="posts-section">
    <div class="posts-container">
        <div class="posts-header">
            <h2 class="section-title">Transmission Log</h2>
            <a href="<%- url_for('/archives') %>" class="view-all-link">
                <span>All Transmissions</span>
                <svg class="icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <line x1="7" y1="17" x2="17" y2="7"></line>
                    <polyline points="7,7 17,7 17,17"></polyline>
                </svg>
            </a>
        </div>
        
        <div class="posts-grid">
            <% site.posts.sort('date', -1).limit(6).each(function(post) { %>
                <article class="post-card">
                    <% if (post.image) { %>
                        <img src="<%= post.image %>" alt="<%= post.title %>" class="post-image">
                    <% } else { %>
                        <div class="post-image" style="background: linear-gradient(135deg, var(--color-blue), var(--color-purple)); display: flex; align-items: center; justify-content: center; color: white; font-size: 2rem;">
                            📝
                        </div>
                    <% } %>
                    
                    <div class="post-content">
                        <div class="post-meta">
                            <time class="post-date" datetime="<%= date(post.date, 'YYYY-MM-DD') %>">
                                <%= date(post.date, 'MMM DD, YYYY') %>
                            </time>
                            <% if (post.categories && post.categories.length) { %>
                                <span class="post-category">
                                    <%= post.categories.data[0].name %>
                                </span>
                            <% } %>
                        </div>
                        
                        <h3 class="post-title">
                            <a href="<%- url_for(post.path) %>" class="post-link">
                                <%= post.title %>
                            </a>
                        </h3>
                        
                        <p class="post-excerpt">
                            <%= truncate(strip_html(post.excerpt || post.content), 120) %>
                        </p>
                        
                        <div class="post-footer">
                            <a href="<%- url_for(post.path) %>" class="read-more">
                                <span>Continue Reading</span>
                                <svg class="icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                    <line x1="7" y1="17" x2="17" y2="7"></line>
                                    <polyline points="7,7 17,7 17,17"></polyline>
                                </svg>
                            </a>
                            
                            <% if (post.tags && post.tags.length) { %>
                                <div class="post-tags">
                                    <% post.tags.limit(3).each(function(tag) { %>
                                        <a href="<%- url_for(tag.path) %>" class="tag">
                                            #<%= tag.name %>
                                        </a>
                                    <% }) %>
                                </div>
                            <% } %>
                        </div>
                    </div>
                </article>
            <% }) %>
        </div>
    </div>
</section>

<!-- Newsletter/CTA Section -->
<section class="cta-section" style="padding: var(--space-24) 0; background: var(--background-secondary);">
    <div class="container">
        <div class="cta-content" style="text-align: center; max-width: 600px; margin: 0 auto;">
            <h2 class="text-large-title" style="margin-bottom: var(--space-4);">
                Join the Mission
            </h2>
            <p class="text-title-3 text-secondary" style="margin-bottom: var(--space-8); line-height: 1.5;">
                Receive priority transmissions about new discoveries and mission updates from across the galaxy.
            </p>
            
            <div class="newsletter-form" style="display: flex; gap: var(--space-3); max-width: 400px; margin: 0 auto;">
                <input 
                    type="email" 
                    placeholder="Enter communication frequency" 
                    class="form-input" 
                    style="flex: 1;"
                    aria-label="Email address"
                >
                <button class="btn btn-primary btn-md">
                    Establish Link
                </button>
            </div>
            
            <p class="text-footnote text-tertiary" style="margin-top: var(--space-4);">
                Secure quantum encryption. Disconnect at light speed.
            </p>
        </div>
    </div>
</section>

<script>
// Add scroll-triggered animations
document.addEventListener('DOMContentLoaded', function() {
    const observerOptions = {
        threshold: 0.1,
        rootMargin: '0px 0px -10% 0px'
    };
    
    const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                entry.target.classList.add('animate-fade-in-up');
            }
        });
    }, observerOptions);
    
    // Observe feature cards and post cards
    document.querySelectorAll('.feature-card, .post-card').forEach(card => {
        observer.observe(card);
    });
    
    // Add stagger effect to feature cards
    const featureCards = document.querySelectorAll('.feature-card');
    featureCards.forEach((card, index) => {
        card.style.animationDelay = `${index * 0.1}s`;
    });
    
    // Add stagger effect to post cards
    const postCards = document.querySelectorAll('.post-card');
    postCards.forEach((card, index) => {
        card.style.animationDelay = `${index * 0.1}s`;
    });
    
    // Newsletter form handling
    const newsletterForm = document.querySelector('.newsletter-form');
    if (newsletterForm) {
        const emailInput = newsletterForm.querySelector('input[type="email"]');
        const subscribeBtn = newsletterForm.querySelector('button');
        
        subscribeBtn.addEventListener('click', function(e) {
            e.preventDefault();
            
            if (emailInput.value && emailInput.validity.valid) {
                // Add success state
                subscribeBtn.textContent = 'Subscribed!';
                subscribeBtn.disabled = true;
                emailInput.value = '';
                
                // Reset after 3 seconds
                setTimeout(() => {
                    subscribeBtn.textContent = 'Subscribe';
                    subscribeBtn.disabled = false;
                }, 3000);
            } else {
                // Add error state
                emailInput.focus();
                emailInput.style.borderColor = 'var(--color-red)';
                
                setTimeout(() => {
                    emailInput.style.borderColor = '';
                }, 2000);
            }
        });
    }
});
</script>